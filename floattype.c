#include <stdio.h>

int main(){

    float x, y, sum;
    printf("Enter the number: ");
    scanf("%f", &x);

    printf("Enter the number: ");
    scanf("%f", &y);

    sum = x + y;

    printf("The sum is: %f", sum);

    return 0;
}
